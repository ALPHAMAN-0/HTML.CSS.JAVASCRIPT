# HTML.CSS.JAVASCRIPT

- No build/test/lint commands found: no `package.json`, `go.mod`, `pyproject.toml`, or `Cargo.toml` at repo root.
- Static learning repo: open a lesson/project's `.html` file directly in a browser (e.g. `HTML Introduction/index.html`) — no dev server observed.
- `API /` and `DOM /` directory names have a trailing space on disk — quote paths carefully in shell commands.
- Files worth reading first:
  - `README.md`
  - `ARCHITECTURE.md`
  - `DOM /OOPS.md`

Architecture: see ARCHITECTURE.md — read before structural changes
